name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Project (Webpack)
        run: npm run build # Bước này tạo ra file 'dist/main.js'

      # BƯỚC MỚI: Đẩy secret DATABASE_URL lên Cloudflare một cách an toàn
      - name: 🔐 Set Cloudflare Secrets
        run: echo "${{ secrets.DATABASE_URL }}" | npx wrangler secret put DATABASE_URL
        env:
          # Bước này cũng cần API token và Account ID để xác thực
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # BƯỚC SỬA LỖI: Đơn giản hóa deploy, để Wrangler đọc file .toml
      - name: 🚀 Deploy to Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # KHÔNG CẦN 'command:' NỮA!
          # Wrangler-action sẽ tự động chạy 'wrangler deploy'.
          # 'wrangler deploy' sẽ tự động đọc file 'wrangler.toml' 
          # và biết phải deploy file 'dist/main.js' với tên 'veolagi'
          # và các compatibility_flags cần thiết.